# ==============================================================================
# ARO — Docker Compose (local development stack)
# ==============================================================================
# Usage:
#   docker compose up --build        # Build and start
#   docker compose up -d             # Start in background
#   docker compose logs -f aro       # Follow logs
#   docker compose down              # Stop all
# ==============================================================================

services:
  aro:
    build: .
    ports:
      - "5000:5000"
    env_file: .env
    environment:
      - FLASK_ENV=production
      - ARO_LOG_LEVEL=INFO
    volumes:
      - ./logs:/app/logs
      - vector_data:/app/vector_store
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Remote MCP server — plug ARO into Claude Desktop / Claude Code / Cursor:
  #   claude mcp add --transport http aro http://localhost:8001/mcp
  aro-mcp:
    build: .
    command: python -m mcp_server.server --http --host 0.0.0.0 --port 8001
    ports:
      - "8001:8001"
    env_file: .env
    volumes:
      - ./logs:/app/logs
      - vector_data:/app/vector_store
    restart: unless-stopped

volumes:
  vector_data:
    driver: local
