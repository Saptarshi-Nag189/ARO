# ── Core Framework ──────────────────
flask==3.1.3
Flask-Cors==6.0.2
pydantic==2.12.5

# ── LangGraph Engine (v3) ───────────
# (bounds match the tested stack: langgraph 1.2.x / langchain 1.x)
langgraph>=1.0,<2
langchain-core>=1.0,<2
langchain-openai>=1.0,<2
langgraph-checkpoint-sqlite>=3.0,<4
langsmith>=0.4

# Optional providers / backends (pluggable, enabled via env vars):
#   pip install langgraph-checkpoint-postgres   # ARO_CHECKPOINT_URI=postgresql://...
#   pip install langchain-aws                   # ARO_MODEL_PROVIDER=bedrock

# ── Networking ──────────────────────
requests==2.32.5
httpx==0.28.1

# ── Graph & Analysis ────────────────
networkx==3.6.1

# ── Vector Memory ───────────────────
chromadb==1.0.0

# ── Web Search ──────────────────────
ddgs>=9.0  # successor of the deprecated duckduckgo-search package
trafilatura==2.0.0
wikipedia==1.4.0

# ── MCP Server ──────────────────────
fastmcp>=2.0

# ── Configuration ───────────────────
python-dotenv==1.2.1
click==8.3.1

# ── Production Server ──────────────
gunicorn==22.0.0

# ── Dev / Testing ───────────────────
pytest>=8.0
